<template><div><h1 id="mapping" tabindex="-1"><a class="header-anchor" href="#mapping" aria-hidden="true">#</a> Mapping</h1>
<h2 id="cartesian-product" tabindex="-1"><a class="header-anchor" href="#cartesian-product" aria-hidden="true">#</a> Cartesian product</h2>
<h3 id="definition" tabindex="-1"><a class="header-anchor" href="#definition" aria-hidden="true">#</a> Definition</h3>
<blockquote>
<p>Given sets A and B, we can define a new set A &gt;× B, called the
Cartesian product of A and B, as a set of ordered pairs. That is,</p>
<p>$$A × B = {(a, b) : a ∈ A, b ∈ B}$$</p>
</blockquote>
<details class="custom-container details"><summary>Example</summary>
<p>Let A = {1, 2, 3}, B = {x, y}, and \ then
$$A × B = {(1, x), (1, y), (2, x), (2, y), (3, x), (3, y)}$$
$$A × C = \emptyset$$</p>
</details>
<p>We define the Cartesian product of n sets to be A1 × A2 × ... × An = {(a1, a2, ..., an) : ai ∈ Ai for i = 1, .., n} If A1 = A2 = ... = An, we write An for A × A.... × A.</p>
<ul>
<li>Example: R^3 consists of all of 3-tuples of real numbers.</li>
</ul>
<h3 id="definition-2" tabindex="-1"><a class="header-anchor" href="#definition-2" aria-hidden="true">#</a> Definition 2</h3>
<blockquote>
<p>Intuitively, an injective (or one-to-one) function never maps distinct
elements of its domain to the same element of its codomain.</p>
</blockquote>
<h2 id="mapping-1" tabindex="-1"><a class="header-anchor" href="#mapping-1" aria-hidden="true">#</a> Mapping</h2>
<h3 id="definition-1" tabindex="-1"><a class="header-anchor" href="#definition-1" aria-hidden="true">#</a> Definition</h3>
<blockquote>
<ul>
<li>Mapping is a proces that each element of a set X (domain) is associated with one and only one
element of a set Y (range/codomain).</li>
<li>Domain: The set of allowed inputs</li>
<li>Codomain: The set of allowed outputs</li>
</ul>
</blockquote>
<ul>
<li>A map is often used as a synonym for a function. Only <b>one-to-one</b> and <b>many-to-one</b> can be called a function.</li>
</ul>
<h3 id="definition-2-1" tabindex="-1"><a class="header-anchor" href="#definition-2-1" aria-hidden="true">#</a> Definition 2</h3>
<blockquote>
<p>Consider the functions f : A → B and g : B → C, the composition of these two functions f ◦ g : A → C</p>
</blockquote>
</div></template>


